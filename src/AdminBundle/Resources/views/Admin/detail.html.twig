{% extends '@AdminBundle/Resources/views/base.html.twig' %}
{% block content %}


<section class="content-header">
    <h1>
        Cliente
    </h1>
</section>

<section class="content">
    <div class="row">
        <div class="box box-primary">
            <div class="box-header with-border">
                <h3 class="box-title">Cadastro</h3>
            </div>
            
            <div class="box-body">
                <strong><i class="fa fa-book margin-r-5"></i> Nome</strong>
                <p class="text-muted">{{user.name}}</p>
                <hr>

                <strong><i class="fa fa-book margin-r-5"></i> E-mail</strong>
                <p class="text-muted">{{user.email}}</p>
                <hr>

                <strong><i class="fa fa-book margin-r-5"></i> CPF</strong>
                <p class="text-muted">{{user.cpf}}</p>
                <hr>

                <strong><i class="fa fa-book margin-r-5"></i> Contato</strong>
                <p class="text-muted">{{user.phone}}</p>
                <hr>

                <strong><i class="fa fa-book margin-r-5"></i> Usuário desde</strong>
                <p class="text-muted">{{user.created|date}}</p>
                <hr>

                {% if user.address %}
                <strong><i class="fa fa-book margin-r-5"></i> Endereço</strong>
                <p class="text-muted">{{user.address.name}} - {{user.address.address}}, {{user.address.number}}</p>
                <hr>
                {% endif %}
                
            </div>
        </div>
    </div>
</section>
{% endblock %}