{% extends '@AdminBundle/Resources/views/base.html.twig' %}
{% block content %}
    <div class="col-md-6">
        <div class="box box-primary">
            <div class="box-header with-border">
                <h3 class="box-title">Cadastro de motorista</h3>
            </div>

            <div class="box-body">
                <div class="form-group">

                    {{ form_start(form) }}
                    {{ form_end(form) }}

                </div>
            </div>
        </div>
    </div>

    <div class="col-md-6">
        <div class="btn-group">
            <a type="button" href="{{ path('go_admin_driver_index') }}" class="btn btn-default btn-flat">
                <i class="fa fa-arrow-left"></i> Listagem
            </a>
        </div>
    </div>

{% endblock %}